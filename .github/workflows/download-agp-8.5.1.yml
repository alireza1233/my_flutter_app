name: Get AGP 8.5.1 and commit to repo

on:
  workflow_dispatch:

jobs:
  download-and-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # اجازه نوشتن در مخزن

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Create Gradle project and resolve AGP 8.5.1
        run: |
          mkdir -p gradle-project
          cd gradle-project
          cat > build.gradle << 'EOF'
          buildscript {
              repositories {
                  google()
                  mavenCentral()
              }
              dependencies {
                  classpath 'com.android.tools.build:gradle:8.5.1'
              }
          }
          apply plugin: 'java'
          repositories {
              google()
              mavenCentral()
          }
          dependencies {
              implementation 'com.android.tools.build:gradle:8.5.1'
          }
          EOF
          gradle dependencies --no-daemon

      - name: Copy AGP 8.5.1 folder into repository
        run: |
          mkdir -p agp-cache/8.5.1
          cp -r ~/.gradle/caches/modules-2/files-2.1/com.android.tools.build/gradle/8.5.1/* agp-cache/8.5.1/

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add agp-cache/
          git commit -m "Add AGP 8.5.1 cache files [skip ci]" || echo "No changes to commit"
          git push origin main
